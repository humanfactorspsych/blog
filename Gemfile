# To install gems:
# 1. if bundler is not installed, install bundler using the command 'gem install bundler'
# 2. add the line 'gem "gem-name"' to this file
# 3. run the command 'bundle install'
# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "jekyll-remote-theme"
gem "jekyll-paginate"
gem 'jekyll-feed'
gem 'jekyll-seo-tag'
